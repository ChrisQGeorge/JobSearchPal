fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
sqlalchemy[asyncio]>=2.0,<3.0
aiomysql>=0.2,<1.0
pymysql>=1.1,<2.0
cryptography>=42,<44
alembic>=1.13,<2.0
pydantic>=2.8,<3.0
pydantic-settings>=2.6,<3.0
email-validator>=2.2,<3.0
passlib[argon2]>=1.7,<2.0
argon2-cffi>=23,<24
python-jose[cryptography]>=3.3,<4.0
python-multipart>=0.0.17,<1.0
openpyxl>=3.1,<4.0
# Document text extraction for PDF / DOCX / HTML uploads.
pypdf>=4.3,<6.0
python-docx>=1.1,<2.0
beautifulsoup4>=4.12,<5.0
html2text>=2024.2,<2026.0
# HTTP client for ATS / RSS source ingest (job_sources poller).
httpx>=0.27,<1.0
# RSS / Atom feed parser used by the rss + yc adapters.
feedparser>=6.0,<7.0
# Websockets for the noVNC stream proxy (R10).
websockets>=13.0,<15.0
# Playwright drives the streamed Chromium over CDP (R10/R11). The
# chromium service runs the browser; this is just the client lib in
# the api container. No separate `playwright install` needed because
# we connect to an already-running Chromium via remote debugging URL.
playwright>=1.46,<2.0
# Markdown → HTML conversion for the server-side PDF render path
# (R11 — apply_run uploads tailored resume/cover-letter PDFs).
markdown-it-py>=3.0,<4.0
